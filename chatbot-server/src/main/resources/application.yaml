spring:

  application:
    name: chatbot
  datasource:
    url: jdbc:mariadb://localhost:3306/ai
    username: root
    password: 1234
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    show-sql: true
server:
  port: 8080

feign:
  httpclient:
    enabled: true
